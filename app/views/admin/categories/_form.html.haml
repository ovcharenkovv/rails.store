= form_for([:admin,@category]) do |f|
  - if @category.errors.any?
    #error_explanation
      %h5
        = pluralize(@category.errors.count, "error")
        prohibited this category from being saved:
      %ul
        - @category.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :dealers_only
    %br/
    = f.check_box :dealers_only
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :ordering
    %br/
    = f.text_field :ordering
  .field
    = f.label :margin
    %br/
    = f.text_field :margin
  .field
    = f.label :min_margin
    %br/
    = f.text_field :min_margin
  .field
    = f.label :description
    %br/
    = f.text_area :description
  .field
    = f.label :parent
    %br
    = f.collection_select :parent_id, Category.root_category , :id, :name,  {:include_blank => 'root'}
  .actions
    = f.submit
    = link_to "Cancel", :back, :class=>"cancel"