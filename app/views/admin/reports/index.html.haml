%h1 Listing reports

%table
  %tr
    %th Name
    %th Param
    %th Action

  - @reports.each do |report|
    %tr
      %td= link_to report.name, [:admin,report]
      %td= report.param
      %td
        = link_to 'Edit', edit_admin_report_path(report)
        =' | '
        = link_to 'Destroy', [:admin,report], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Report', new_admin_report_path
