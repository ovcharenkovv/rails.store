%h1 Listing users
.rest_buttons
  = link_to 'Authors', admin_authors_path,:class=>"cool_buton"
%table
  %tr
    %th id
    %th email
    %th encrypted_password
    %th author
    %th role
    %th created_at
    %th action

  - @users.each do |user|
    %tr
      %td=user.id
      %td=link_to user.email, edit_admin_user_path(user)
      %td=user.encrypted_password
      %td=user.author.name unless user.author.nil?
      %td=user.role
      %td=user.created_at.strftime("%Y-%m-%d %H:%M")
      %td= link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete
