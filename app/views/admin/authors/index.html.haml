%h1 Listing authors
.rest_buttons
  = link_to 'New Author', new_admin_author_path,:class=>"cool_buton"
%table
  %tr
    %th Nik
    %th Full Name
    %th Email
    %th Phone
    %th City
    %th CCN
    %th.action
  - @authors.each do |author|
    %tr
      %td= link_to author.name, admin_author_products_path(author)
      %td= author.fullname
      %td= author.email
      %td= author.phone
      %td= author.city
      %td= author.ccn
      %th
        = link_to 'Edit', edit_admin_author_path(author)
        = ' / '
        = link_to 'Destroy', admin_author_path(author), :confirm => 'Are you sure?', :method => :delete